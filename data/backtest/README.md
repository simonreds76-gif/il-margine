# Backtest and Internal Signal Ledgers

`strict-signals-challenger-ml-*` and `strict-signals-clay-fav-*` are internal research ledgers.
They are for localhost/model-monitor analysis only and must not be mirrored to public data
artifacts or public website routes without an explicit promotion decision.

`backtest-results-challenger-2026.csv` is a historical research artifact with separate
provenance. Do not append live Challenger shadow signals to it.
